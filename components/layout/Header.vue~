<script setup lang="ts">
import {useExtraDataStore} from "~/store/extra-data";

const extraDataStore = useExtraDataStore();
</script>

<template>
  <NLayoutHeader class="rounded-xl p-4 m-4 shadow-sm">
    <div class="flex justify-between">
      <div class="flex items-center">
        <Icon v-if="extraDataStore.internetOnline" name="tabler:wifi" size="24" class="text-green-500"/>
        <Icon v-else name="solar:wifi-off" class="mr-2"/>
        <span class="text-sm font-light text-gray-700">
          {{ extraDataStore.internetOnline ? 'Connected' : 'Not connected' }}
        </span>
      </div>
    </div>
  </NLayoutHeader>
</template>

<style scoped>

</style>